.[service]
pytest

# The Docker image uses requirements.docker.txt instead of this file: it
# resolves entirely offline from the first-party sdist registry plus whatever
# the base image preinstalls, and pytest is not available that way. See
# requirements.docker.txt.
