version: "3.9"

services:
  rabbitmq:
    image: "rabbitmq:3.12-management"
    container_name: cltl-client-rabbitmq
    ports:
      - "5672:5672"    # AMQP port
      - "15672:15672"  # Management UI port
    environment:
      - RABBITMQ_DEFAULT_USER=cltl
      - RABBITMQ_DEFAULT_PASS=cltl123
      - RABBITMQ_DEFAULT_VHOST=/
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 10s
      retries: 6
    networks:
      - cltl-client-network

  cltl-client-app:
    image: cltl/client-app
    build:
      context: ../py-app
      dockerfile: ../docker-app/Dockerfile
    container_name: cltl-client-app
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
      - ./storage:/app/storage
    environment:
      - CLTL_LOGGING_CONFIG=/app/config/logging.config
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - cltl-client-network
    extra_hosts:
      # On Linux, map the special name to the host gateway.
      # On Mac/Windows it's already defined; this line is harmless there.
      - "host.docker.internal:host-gateway"

volumes:
  rabbitmq_data:

networks:
  cltl-client-network:
    driver: bridge
